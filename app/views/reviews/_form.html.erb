<%= form_for([@activity, @activity.reviews.new]) do |f| %>
    <%= f.text_field :star %>
    <%= f.text_field :comment %>
    <%= f.hidden_field :room_id, value: @activity.id %>
    <div class = "actions">
        <%= f.submit %>
    </div>
<% end %>
